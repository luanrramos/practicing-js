function sum(n1, n2) {
  // Função padrão
  return n1 + n2;
}

const mult = (n1, n2) => n1 * n2; // Com arrow function


// Objetivo: Utilizar essas funções e objetivo que criei em outra pasta .js

module.exports = {sum, mult}
